{% extends 'baseAdmin.html.twig' %}

{% block title %}Administration | Réalisations | {{ parent() }}{% endblock %}
{% block body %}
    <div style="margin-top: 3%;"></div>
    <div class="container-fluid row">
        <div class="col-lg-3"></div>
        <div class="col-lg-6" style="background-color: rgba(238,238,238,0.33); border-radius: 10px; padding: 1% 1% 1% 1%;">
            {% form_theme form 'bootstrap_4_layout.html.twig' %}
            {{ form(form) }}
            <a href="{{ url('admin_realisations') }}" class="btn-danger btn">Retour à la liste</a>
        </div>
        <div class="col-lg-3"></div>
    </div>
{% endblock %}
